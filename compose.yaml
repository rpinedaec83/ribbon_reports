services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Z@rverus11
    ports:
      - '5432:5432'
    volumes:
      - ribbon-reports:/var/lib/postgresql/data
volumes:
  ribbon-reports:
